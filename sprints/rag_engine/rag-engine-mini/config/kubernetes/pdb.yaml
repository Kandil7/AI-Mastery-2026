apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rag-engine-pdb
  namespace: rag-engine
spec:
  minAvailable: 1
  maxUnavailable: 0
  selector:
    matchLabels:
      app: rag-engine
